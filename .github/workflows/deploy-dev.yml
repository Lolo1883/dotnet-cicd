name: Deploy to Dev

on:
  pull_request:
    branches:
      - main

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Dev
        run: echo "Deployed to dev (PR #${{ github.event.number }} - ${{ github.head_ref }})"
